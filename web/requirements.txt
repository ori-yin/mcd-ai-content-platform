# MCD AI 内容平台 - Web 骨架依赖（FastAPI + Jinja2 + HTMX）
# 安装：pip install -r requirements.txt
# 启动：uvicorn app:app --reload --port 8530

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
# HTMX 通过 CDN 引入（见 templates/base.html），无需 pip 安装