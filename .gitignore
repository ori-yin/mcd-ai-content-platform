# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Streamlit
.streamlit/secrets.toml
.streamlit/config.toml

# 环境与密钥
.env
*.pem
*.key

# 测试与缓存
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# 数据与日志
*.log
logs/
data/*.db
data/*.sqlite
data/*.sqlite3
data/cnn_backup_cleaned.xlsx
data/cnn_backup_cleaned.parquet

# 临时文件
*.bak
_*.py
!**/__init__.py
*.tmp
temp/

# 本地 OneDrive 同步残留（按 memory feedback-onedrive-git）
# 如果在 OneDrive 下，看到 .git 被吞，恢复流程参考 Handoff.md §13

# L1 LightGBM 模型文件（pkl 二进制，重训可复现，不入 git）
data/lgbm_model_*.pkl

# 漂移监控日志（运行时累积，定期清理）
data/drift_log.csv

# 特征重要性月报运行时落档（每次跑都生成，按月归档不入 git）
data/feature_importance_history/
data/reports/

# 一次性 GitHub Contents API 推送脚本（不入 git，需要时本地重生成）
tools/push_*_via_api.py

# 字典本地备份（防手残误删，详见 tools/backup_dicts.py）
data/.backups/
