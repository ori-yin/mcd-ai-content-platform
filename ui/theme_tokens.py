# -*- coding: utf-8 -*-
r"""
ui/theme_tokens.py — 麦当劳品牌配色 + 设计 token

复用自 C:\ideon\mcd-copy-analyzer\config.py（颜色常量部分）
新项目不直接 import 旧项目，token 在此独立维护

按 CLAUDE.md §4.3 数据契约：颜色/字号/间距/圆角集中维护，不散落 UI 文件

【v2 视觉重构】品牌色降级为 accent，中性色建立结构，scale token 化
"""

from __future__ import annotations


# ============================================================
# 麦当劳品牌色（色值锁定，仅 KFC/市场部调整）
# 用法约束：禁做大面积背景；仅用于主操作、选中态、KPI 数字、状态竖条
# ============================================================

MCD_RED = "#DA291C"        # 麦当劳红（主品牌色，慎用大面积）
MCD_GOLD = "#FFC72C"       # 麦当劳金（强调；不要做正文色，对比度仅 1.6:1）
MCD_DARK_RED = "#A11918"   # 深红（hover / 主操作按压）
MCD_GREEN = "#008000"      # 通过项 / 正向指标
MCD_YELLOW = "#FFC72C"     # 警告项（同金色）
MCD_BORDER = "#E0E0E0"     # 默认边框（保留兼容；新代码用 GRAY_200）

# 兼容别名（pages/00_home.py 等旧调用点）
MCD_BG = "#FFFFFF"
MCD_BG_DARK = "#1E1E1E"
MCD_GRAY = "#5C5C5C"
MCD_LIGHT_GRAY = "#F5F5F5"


# ============================================================
# 中性色阶（v2 新增；建立结构的主力）
# 全部 WCAG AA 对比度；GRAY_500 配白底 4.6:1，GRAY_400 仅做 disabled/placeholder
# ============================================================

GRAY_50  = "#FAFAFA"   # 页面底色 / 表头底
GRAY_100 = "#F4F4F4"   # 行 hover / 浅卡底
GRAY_200 = "#E8E8E8"   # 主边框 / 分割线
GRAY_300 = "#D4D4D4"   # 次要边框 / 输入框
GRAY_400 = "#A3A3A3"   # 占位符 / 禁用文字
GRAY_500 = "#737373"   # 次要文字 / 标签
GRAY_600 = "#525252"   # 正文
GRAY_700 = "#404040"   # 强调正文
GRAY_900 = "#171717"   # 标题


# ============================================================
# 字号 scale（5+2 档；禁止裸值；单位 px）
# ============================================================

TEXT_XS    = "12px"   # 标签、meta、时间戳
TEXT_SM    = "13px"   # 辅助说明、表格次要列
TEXT_BASE  = "14px"   # 正文、表格
TEXT_MD    = "15px"   # 卡片标题、表头
TEXT_LG    = "18px"   # 页面标题
TEXT_XL    = "24px"   # KPI 数字
TEXT_2XL   = "32px"   # 首页大数字（极少用）


# ============================================================
# 间距 scale（6 档；禁止裸值；单位 px）
# ============================================================

SPACE_1 = "4px"
SPACE_2 = "8px"
SPACE_3 = "12px"
SPACE_4 = "16px"
SPACE_6 = "24px"
SPACE_8 = "32px"


# ============================================================
# 圆角（3 档；最大 8px；工具类界面不超过 8）
# ============================================================

RADIUS_SM = "4px"   # 标签、badge、input
RADIUS_MD = "6px"   # 按钮、卡片、弹窗
RADIUS_LG = "8px"   # 大容器、header


# ============================================================
# 阴影（仅留给真正浮起的元素）
# v2 删除 SHADOW_SM/MD：静态层不再用 shadow 分层，改用 1px 边 + 背景色差
# ============================================================

SHADOW_FLOAT = "0 4px 12px rgba(0,0,0,0.08), 0 1px 3px rgba(0,0,0,0.04)"


# ============================================================
# Plotly 分类色板（替换 PLOTLY_SEQUENCE）
# 原则：明度递减 + 色盲可辨（避开红绿配对，红系列改深蓝区分）
# ============================================================

PLOTLY_PRIMARY = MCD_RED
PLOTLY_SECONDARY = "#1F6FEB"     # 深蓝（次主系列）
PLOTLY_NEUTRAL = MCD_GRAY

PLOTLY_SEQUENCE = [
    MCD_RED,        # 主系列
    "#1F6FEB",      # 深蓝
    "#FF8C00",      # 橙
    MCD_GREEN,      # 绿（仅正向指标用，避免与红系列同图）
    "#8B5CF6",      # 紫（参考线/预测）
    GRAY_500,       # 中灰（参考/历史）
]


# ============================================================
# 字体
# ============================================================

FONT_FAMILY = '"PingFang SC", "Microsoft YaHei", "Helvetica Neue", sans-serif'