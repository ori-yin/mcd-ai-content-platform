# MCD AI 内容运营工作台需求文档

> 文档版本：v2.0  
> 项目阶段：内部 Demo / MVP  
> 推荐技术：Streamlit + Python  
> 文档用途：直接交给 Claude Code 进行代码审阅、架构整合和开发  
> 核心原则：保留领导 Demo 主流程，复用现有项目能力，前台统一、底层模块化

---

## 0. 给 Claude Code 的首要说明

本项目不是从零开发一个孤立的 AI 文案生成器。

开发前必须先审阅以下两个现有项目：

1. `mcd-copy-analyzer`
2. `mcd-ctr-predictor` 或当前实际维护的 `mcd-ctr-predictor-main`

新项目的产品入口需要统一，但底层能力必须模块化。禁止将三个项目简单复制粘贴到一个巨大的 `app.py` 中。

领导提供的 Demo 是本项目的核心业务构想，必须优先保留：

```text
左侧：定义经营任务
中间：生成并选择候选内容
右侧：渠道预览、CTR 参考和解释结果
```

除非某项功能存在明显的数据真实性、合规、技术不可行或重复建设问题，否则不得擅自删除、弱化或改变领导 Demo 中的核心功能和交互。

如果实现方式需要调整，应做到：

- 保留用户可见的功能目标。
- 调整底层实现，而不是直接删除功能。
- 对暂时缺乏真实数据支撑的结果明确标注。
- 将无法在 MVP 完成的部分保留为接口、占位和后续计划。

---

# 1. 项目背景

当前已经存在两个相关工具：

## 1.1 mcd-copy-analyzer

定位为历史文案分析与单条文案诊断工具，现有能力可能包括：

- Excel 数据读取与字段映射。
- 从业务数据中抽取标题与正文。
- 文本清洗、jieba 分词、自定义词典和停用词。
- 词语、Emoji、字数等表达分析。
- 含某词与不含某词的 CTR 对比。
- 本地文案诊断。
- 历史相似 Plan 检索。
- 高效 Plan、趋势和 Owner 等分析。
- AI 深度改写。

Claude Code 必须以实际仓库代码为准确认以上能力，不得仅凭本 PRD 假设函数、接口或文件一定存在。

## 1.2 mcd-ctr-predictor

定位为 CTR 基准参考、批量预测和优化建议工具，现有能力可能包括：

- 上传 CSV 或 Excel 文案列表。
- CTR 基准数据读取。
- 按渠道、是否用券、Plan 类型、工作日类型、标题字数等维度匹配基准。
- LLM 批量预测或评估。
- 文案优化建议。
- 结果下载。
- CTR 基准校准脚本和测试。

Claude Code 必须以当前实际维护版本为准，确认 `mcd-ctr-predictor` 与 `mcd-ctr-predictor-main` 的关系，选择真实有效的代码作为整合来源。

## 1.3 新需求

领导希望增加一个 AI 自动生成营销内容的可视化 Demo：

1. 用户选择人群、投放渠道、产品与权益、投放目标、活动阶段和消费场景。
2. AI 自动生成多条候选文案。
3. 用户从候选文案中选择一个版本。
4. 页面最右侧展示渠道预览、参考 CTR、投放理由和预测置信信息。

新项目应把三个环节串成闭环：

```text
历史洞察与规则
mcd-copy-analyzer
        ↓
营销内容生成与选择
新内容创作中心
        ↓
CTR 基准与预测参考
mcd-ctr-predictor
        ↓
人工编辑、选择、保存与后续效果回流
```

---

# 2. 产品定位

## 2.1 产品名称

暂定：`MCD AI 内容运营工作台`

可选英文代码名：`mcd-ai-content-platform`

## 2.2 一句话定位

将历史内容洞察、AI 文案生成、规则诊断和 CTR 参考连接在一个工作台中，帮助业务人员从经营任务出发，完成候选生成、比较、预览和人工决策。

## 2.3 核心价值

本产品不是普通的 AI 聊天框，而是结构化的内容决策工作流：

```text
业务人员定义任务
→ 系统调用历史规则和内容证据
→ AI 生成差异化候选
→ 系统检查风险并给出 CTR 参考
→ 业务人员编辑和选择
→ 保留完整记录
```

---

# 3. 产品原则

## 3.1 保留领导 Demo

领导 Demo 中以下能力属于 P0，不得删除：

- 三栏式主页面。
- 产品与权益输入。
- 目标人群选择。
- 投放渠道选择。
- 投放目标选择。
- 活动阶段选择。
- 消费场景选择。
- 一键生成多条候选内容。
- 中间区域展示至少 3 条候选文案。
- 用户选择候选版本。
- 最右侧展示渠道预览。
- 最右侧展示 CTR 参考结果。
- 最右侧展示投放理由。
- 最右侧展示可信程度或置信说明。

## 3.2 不伪造预测能力

如果当前 CTR Predictor 无法形成经过验证的预测区间或准确率：

- 保留领导希望看到的 CTR 卡片。
- 将名称写成“CTR 参考”或“预测 CTR 参考”。
- 展示历史基准、预测值、适用维度和数据状态。
- 不得伪造准确率。
- 不得把随机数包装成真实预测。
- Demo 模式必须醒目标注“演示数据，非正式投放承诺”。

## 3.3 AI 只生成候选

AI 结果不得自动进入正式投放。最终选择和编辑由业务人员完成。

## 3.4 硬规则由程序判断

以下规则不能只依赖 LLM：

- 渠道字数。
- 必填字段。
- 禁用词或风险词。
- 未经输入的数字、日期、优惠和库存信息。
- 重复文案。
- 格式错误。
- 是否存在阻断项。

## 3.5 前台统一，底层模块化

用户看到一个工具和统一导航；开发上保留独立模块、服务和适配器。

---

# 4. 产品范围与导航

工作台采用四个主要业务页面和一个设置入口。

## 4.0 CTR 预测三入口

CTR 参考能力不限于"AI 生成后才预测"，用户在三种场景下都能拿到参考：

- **入口 A（创作页，01 内容创作）**：AI 生成 3 条候选后，每条候选自动调用 CTR Adapter，返回历史基准 / 预测值 / 置信度 / 不可用原因
- **入口 B（诊断页，02 文案诊断）**：用户手动输入标题+正文，无需 AI 生成，立刻调用 CTR Adapter 给出参考。适用于"业务人员自己写好文案，问这个值不值得发"
- **入口 C（批量评估页，03 批量评估）**：上传 CSV/Excel 批量文案清单，逐条 CTR 预测 / 优化建议 / 字数建议 / 时段建议，导出结果

三入口共用同一份 CTR Adapter（PRD §12.2 统一接口），保证预测口径一致。

入口 B 与入口 C 的存在意味着 CTR Adapter 必须能脱离"AI 生成上下文"独立工作：仅根据标题+正文+渠道+维度字段返回 PredictionResult。

---

工作台采用四个主要业务页面和一个设置入口。

```text
MCD AI 内容运营工作台
├── 01 内容创作
├── 02 文案诊断
├── 03 批量评估
├── 04 历史洞察
└── 设置
```

## 4.1 01 内容创作

领导 Demo 对应的核心页面，也是默认首页。

```text
定义经营任务
→ 生成 3 条候选
→ 查看历史证据与规则诊断
→ 比较 CTR 参考
→ 渠道预览
→ 人工编辑和选择
→ 保存
```

## 4.2 02 文案诊断

复用 `mcd-copy-analyzer` 中与单条文案直接相关的能力：

- 输入标题和正文。
- 本地规则检查。
- 词语表现参考。
- 历史相似 Plan。
- AI 改写。

## 4.3 03 批量评估

复用两个现有项目的批量能力：

- 上传 CSV 或 Excel。
- 批量解析文案。
- 批量规则检查。
- 批量 CTR 参考或预测。
- 批量优化建议。
- 下载结果。

## 4.4 04 历史洞察

保留但不塞入创作首页的分析功能：

- 高效 Plan 排行。
- 高表现和低表现词语。
- Emoji 表现。
- 标题字数表现。
- 历史相似内容。
- 每日趋势。
- Owner 对比。

## 4.5 设置

- 品牌词典。
- 停用词与禁用词。
- 渠道规则。
- Prompt 版本。
- 模型配置。
- CTR 基准版本。
- Demo / Internal LLM 模式。

设置入口在 MVP 可只读或使用配置文件实现，不要求第一版完成复杂后台管理。

---

# 5. P0 主页面：内容创作

## 5.1 页面布局

忠实保留领导 Demo 的三栏布局。

建议桌面比例：

```text
左栏 22% | 中栏 32% | 右栏 46%
```

响应式要求：

- 宽屏保持三栏。
- 较窄屏可以纵向排列。
- 内网桌面使用体验优先。

---

# 6. 左栏：定义经营任务

## 6.1 页面标题

```text
1 定义经营任务
```

## 6.2 字段

| 字段 | 类型 | 必填 | P0 默认候选或说明 |
|---|---|---:|---|
| 产品类别 | 单选（yaml） | 否 | 来自 config/product_benefit.yaml（Phase A.1 拆分）；yaml 维护 + 自定义兜底 |
| 权益类型 | 单选（yaml） | 否 | 来自 config/product_benefit.yaml（Phase A.1 拆分）；yaml 维护 + 自定义兜底 |
| 目标人群 | 单选 | **是** | 常规大盘、新品兴趣人群、近期活跃用户、沉默召回人群、高价值会员 |
| 投放渠道 | 单选 | **是** | APP Push、企微 1v1、短信、微信小程序订阅消息（Phase 12 渠道清理） |
| 投放目标 | 单选 | 否（灰态） | 建立认知、提升点击、促进领券、促进下单、用户召回、新品种草；首项「通用（不指定）」 |
| 活动阶段 | 单选 | **否** | 活动预热、活动上线、活动爆发、活动收尾；首项「通用（不指定）」（Phase 28 改可选） |
| 消费场景 | 单选 | 否 | 早餐、午餐、下午茶、晚餐、夜宵、周末聚会、其他；首项「通用（不指定）」（Phase 12 #10 改选填） |
| 内容语气 | 单选 | **是** | 直接利益型、场景种草型、品牌互动型、行动号召型 |
| 期望动作 | 单选 | 否 | 点击、领券、下单、回流、到店、查看详情；首项「通用（不指定）」 |
| Plan 类型 | 单选 | 取决于预测逻辑 | AARRPlan、常规Plan、未知；首项「通用（不指定）」 |
| 是否用券 | 单选 | 取决于预测逻辑 | 是、否、未知；首项「通用（不指定）」 |
| 文案含券词 | 单选 | 否 | 否、是；首项「通用（不指定）」；由 `classify_coupon_in_text` 推断（Phase 12 #11） |
| 计划投放日期 | 单选 | 否 | 工作日、非工作日；首项「通用（不指定）」；Phase 11 由 date_input 改为 selectbox |
| 额外要求 | 多行文本 | 否 | 必须出现、不得出现、法务或业务补充信息 |

**Phase 28 必填口径改 3 项**（2026-09-01 用户拍板）：只「目标人群 / 投放渠道 / 内容语气」3 项必填，其余全部可选。理由：阶段、场景等是 LLM 文案素材，可走默认「通用（不指定）」让 AI 自由发挥；硬必填的判定标准是"下游 pipeline 不填跑不通"。

「通用（不指定）」语义：表单可选字段首项 + 默认选中；后端 prompt 拼装遇此值跳过该行；Demo 占位候选遇此值跳过拼接具体动作词。

说明：

- 领导 Demo 中已有的六个核心维度必须保留（必填从 6 缩到 3：人群 / 渠道 / 语气）。
- Plan 类型、是否用券、计划投放日期用于与 CTR Predictor 的基准维度衔接。
- 如果现有 CTR Predictor 不需要某字段，可隐藏在"更多预测条件"折叠区，避免破坏领导 Demo 的简洁视觉。

## 6.3 生成按钮

按钮文案：

```text
生成多条候选内容
```

交互：

1. 校验必填字段。
2. 保存当前任务快照。
3. 调用生成服务生成 3 条候选。
4. 对候选执行本地规则检查。
5. 查询相似历史内容。
6. 调用 CTR 参考服务。
7. 默认选中候选 A。
8. 更新中栏和右栏。

不得因预测服务失败而导致整个生成流程失败。预测不可用时，候选生成和规则诊断仍应正常展示。

## 6.4 说明卡片

左栏底部显示：

> 系统将结合经营任务、渠道规则、历史内容表现和品牌要求生成候选内容。AI 结果仅作为候选，正式投放前仍需人工确认。

---

# 7. 中栏：选择候选内容

## 7.1 页面标题

```text
2 选择候选内容
```

## 7.2 候选方案

每次默认生成 3 条，策略必须明显不同：

| 候选 | 默认策略 | 目的 |
|---|---|---|
| A | 核心利益直给 | 快速说明产品或权益 |
| B | 消费场景切入 | 从具体场景和用户需求进入 |
| C | 行动号召强化 | 强化目标动作，但不得虚构紧迫性 |

如果用户选择特定语气，语气影响三条文案表达，但不得破坏三种策略差异。

## 7.3 候选卡片内容

每张候选卡片展示：

- A、B 或 C 标签。
- 策略名称。
- 标题。
- 正文。
- 生成理由。
- 标题字数。
- 正文字数。
- 规则状态：通过、提醒或阻断。
- CTR 参考值或“暂不可用”。
- 是否经过人工编辑。

## 7.4 选择机制

- 使用单选机制，一次只选择一条。
- 所选卡片使用金色描边和明显状态。
- 切换候选时，右侧立即刷新预览和结果。
- 切换候选不得重新调用生成服务。
- 切换候选不得丢失其他候选的人工编辑结果。

## 7.5 候选操作

P0：

- 选择此版本。
- 编辑标题。
- 编辑正文。
- 恢复 AI 原文。

P1：

- 仅重新生成此版本。
- 缩短。
- 更直接。
- 更有场景感。
- 强化行动号召。
- 根据风险提示改写。
- 根据历史高表现表达改写。

现有 `mcd-copy-analyzer` 中的“AI 深度改写”应逐步收敛为统一的候选改写服务，避免同一产品中存在两套不同的 AI 改写逻辑。

## 7.6 历史证据

候选卡片可展示简要历史证据：

```text
相似历史内容：3 条
历史参考 CTR：有数据时显示
主要相似表达：限定、新品、早餐
```

要求：

- 数据必须来自历史检索服务。
- 没有数据时显示“未找到足够相似的历史内容”。
- 不得由 LLM 编造历史证据。
- 详细内容可以放在候选卡片折叠区，避免页面过重。

## 7.7 候选差异检查

系统检查 A、B、C 的相似度。

如果差异不足，显示：

> 当前候选表达较为接近，建议重新生成差异更大的版本。

MVP 可复用现有 TF-IDF 能力或使用独立文本相似度服务。

---

# 8. 右栏：预览并比较预测结果

## 8.1 页面标题

```text
3 预览并比较预测结果
```

## 8.2 渠道预览

右侧顶部必须保留领导 Demo 中的大尺寸预览卡片。

### APP Push

展示：

- App 图标。
- 标题。
- 正文。
- 模拟时间。
- 通知卡片样式。

### 企微 1v1

展示：

- 品牌发送方占位。
- 对话气泡。
- 正文。
- 链接或卡片占位。

### 短信

展示：

- 短信气泡。
- 正文。
- 字数。
- 如果现有规则支持，显示预计短信段数。

### 站内信

展示：

- 标题。
- 内容摘要。
- 查看详情入口。

P0 至少完成 APP Push；如果领导演示指定其他渠道，则对应渠道提升为 P0。

## 8.3 结果卡片

右侧预览下方保留三个主要结果卡片。

### 卡片一：CTR 参考结果

优先显示：

```text
预测 CTR：0.xx%
历史基准：0.xx%
相对基准：+/- 0.xx 个百分点
```

实际字段必须以现有 CTR Predictor 能力为准。

如果暂时只能提供基准而不能提供有效预测：

```text
历史参考 CTR：0.xx%
预测状态：当前仅提供基准参考
```

如果无有效数据：

```text
CTR 参考：暂不可用
原因：当前条件下无足够基准数据
```

禁止通过纯随机数补齐正式结果。

### 卡片二：投放理由

展示所选文案为什么与任务匹配，例如：

- 权益表达清晰。
- 符合活动预热阶段。
- 内容与早餐场景匹配。
- 行动目标为点击，文案包含明确查看动作。

投放理由可由规则结果和 LLM 解释共同生成，但必须区分事实和建议，不得把建议写成已经验证的结论。

### 卡片三：可信程度

领导 Demo 中的“预测准确度”建议保留视觉位置，但产品文案调整为更真实的“参考可信度”或“预测可信度”。

推荐显示：

```text
参考可信度：高 / 中 / 低
```

解释依据包括：

- 是否命中完整基准维度。
- 历史样本是否充足。
- 是否找到相似历史文案。
- 是否存在缺失输入。
- 当前使用的是基准、模型预测还是 Demo 数据。

如果现有项目没有经过验证的准确率，不得展示“77% 准确率”之类数字。

## 8.4 规则诊断

右侧下方展示：

- 通过项。
- 提醒项。
- 阻断项。
- 修改建议。

颜色：

- 绿色：通过。
- 黄色：提醒。
- 红色：阻断。

颜色不能作为唯一信息，必须同时展示文字和图标。

## 8.5 推荐结论

推荐文案模板：

```text
参考结论：当前版本符合 APP Push 基础规则，与“活动预热 + 建立认知”目标匹配。CTR 结果为历史基准和模型参考，不代表正式投放承诺，建议结合业务判断后使用。
```

如果存在阻断项，不得推荐正式使用。

---

# 9. 文案生成服务

## 9.1 输入 Schema

```json
{
  "product_benefit": "Chiikawa 合作款、限定小卡、无券",
  "audience": "常规大盘",
  "channel": "APP Push",
  "objective": "建立活动认知、提升点击",
  "campaign_stage": "活动预热",
  "consumption_scene": "早餐",
  "tone": "场景种草型",
  "expected_action": "点击",
  "plan_type": "普通 Plan",
  "coupon": "否",
  "planned_send_date": null,
  "extra_requirements": "不得使用免费领取"
}
```

## 9.2 输出 Schema

```json
{
  "request_id": "uuid",
  "candidates": [
    {
      "id": "A",
      "strategy": "核心利益直给",
      "title": "Chiikawa小卡要来了",
      "body": "早餐时光加一点可爱，限定合作款即将上线，点击查看活动信息。",
      "reason": "直接突出合作款和限定权益，适合活动预热阶段建立认知。",
      "risk_flags": [],
      "used_input_fields": [
        "product_benefit",
        "campaign_stage",
        "consumption_scene",
        "expected_action"
      ]
    }
  ],
  "model_info": {
    "provider": "demo_or_internal",
    "model": "model-name",
    "prompt_version": "v1.0"
  }
}
```

## 9.3 输出校验

- 必须返回 3 条候选。
- 候选 ID 必须为 A、B、C。
- 三条策略不得重复。
- 标题和正文不得为空。
- 返回必须符合 JSON Schema。
- 程序允许有限的一次格式修复。
- 修复失败时显示明确错误，不展示残缺结果。

---

# 10. Prompt 设计

## 10.1 System Prompt

```text
你是一名 CRM 营销内容策略助手。

请根据结构化经营任务生成三条差异化候选内容。

必须遵守：
1. 只使用输入中已经确认的信息。
2. 不得虚构优惠力度、活动时间、库存、领取资格、活动机制或产品功效。
3. 三条候选分别采用：核心利益直给、消费场景切入、行动号召强化。
4. 三条候选必须有明显差异，不能只替换近义词。
5. 标题和正文必须符合指定渠道的表达方式。
6. 不得使用绝对化、欺骗性或无法验证的表达。
7. 信息不足时使用中性表达，不得擅自补充事实。
8. 只返回符合约定 Schema 的 JSON。
```

## 10.2 历史洞察注入

如果历史检索服务可用，可将经过筛选的历史证据注入 Prompt：

```text
以下内容仅作为表达参考，不得复制，也不得将历史活动机制带入当前任务：
- 高表现表达摘要
- 低表现或风险表达摘要
- 相似历史文案摘要
```

要求：

- 控制历史内容长度。
- 明确历史事实与当前任务的边界。
- 禁止将历史优惠、日期、库存等事实迁移到新文案。
- 注入内容必须记录来源和版本。

## 10.3 Prompt 版本管理

- Prompt 放在独立目录。
- 每次修改升级版本号。
- 保存记录时记录 Prompt 版本。
- 页面代码不得直接拼接大段 Prompt。

---

# 11. 文案诊断与规则引擎

## 11.1 优先复用的现有能力

Claude Code 应检查 `mcd-copy-analyzer` 是否可复用：

- 文本清洗。
- 标题与正文抽取。
- jieba 自定义词典。
- 停用词和禁用词。
- 本地诊断。
- 文本相似度。
- AI JSON 解析。

不得假设可以直接 import。应先评估：

- 是否依赖 Streamlit 全局状态。
- 是否与页面代码强耦合。
- 是否依赖固定文件路径。
- 是否有单元测试。
- 是否需要抽成纯 Python 服务。

## 11.2 规则项目

至少包含：

1. 渠道标题长度。
2. 渠道正文长度。
3. 是否体现产品或权益。
4. 是否存在期望动作。
5. 是否命中禁用词。
6. 是否包含输入未提供的数字。
7. 是否包含输入未提供的日期。
8. 是否包含未确认的优惠、免费、库存或稀缺承诺。
9. 是否使用绝对化表达。
10. 是否有重复标点、异常空格或格式问题。
11. 候选间是否高度相似。

## 11.3 规则级别

```text
pass：通过
warning：提醒，允许人工继续
block：阻断，不得显示“建议投放”
```

## 11.4 配置化

渠道规则、禁用词和品牌词典不得散落在页面代码中。

建议：

```text
config/channel_rules.yaml
config/brand_rules.yaml
data/custom_dict.txt
data/stopwords.txt
```

---

# 12. CTR 参考服务

## 12.1 复用目标

优先复用 `mcd-ctr-predictor` 的：

- CTR 基准读取。
- 基准维度匹配。
- 基准校准逻辑。
- 文案预测调用。
- 优化建议。
- 测试用例。

## 12.2 统一适配器

```python
class CTRPredictionAdapter:
    def predict(self, task, candidate):
        """返回统一 PredictionResult，不向 UI 暴露旧项目内部结构。"""
```

## 12.3 统一返回结构

```json
{
  "available": true,
  "result_type": "model_prediction",
  "predicted_ctr": 0.0023,
  "baseline_ctr": 0.0021,
  "low": null,
  "high": null,
  "confidence_level": "medium",
  "confidence_reason": [
    "匹配渠道与是否用券基准",
    "未提供计划投放日期"
  ],
  "baseline_dimensions": {
    "channel": "APP Push",
    "coupon": "否",
    "plan_type": "普通 Plan",
    "day_type": null,
    "title_length_bucket": "11-15字"
  },
  "sample_size": null,
  "model_version": "",
  "baseline_version": ""
}
```

## 12.4 result_type

允许值：

- `model_prediction`：真实调用现有预测逻辑。
- `baseline_only`：只提供历史基准。
- `demo`：演示结果。
- `unavailable`：无有效结果。

页面必须根据 `result_type` 使用不同标签，不能混淆。

## 12.5 置信区间

只有现有预测逻辑明确返回经过定义的上下界时，页面才显示预测区间。

如果只有单点预测：

- 显示预测值。
- 显示历史基准。
- 不自行创建统计置信区间。

## 12.6 参考可信度

如果没有正式校准模型，可信度可依据数据完整性分级，但必须明确为“参考可信度”，不是“预测准确率”。

示例：

- 高：完整命中基准维度，样本信息充足，且找到相似历史内容。
- 中：命中主要维度，但存在部分缺失。
- 低：仅能使用宽泛渠道基准或 Demo 数据。

具体分级规则应配置化，并在代码和页面中可解释。

---

# 13. 现有功能取舍

## 13.1 创作首页必须保留

- 领导 Demo 的三栏主流程。
- 六个核心经营任务维度。
- 三条候选文案。
- 候选选择。
- 渠道预览。
- CTR 参考结果。
- 投放理由。
- 可信程度。
- 人工编辑。

## 13.2 迁移到独立页面

以下功能有价值，但不应堆到创作首页：

- 高效 Plan 排行。
- 每日趋势。
- Owner 对比。
- 完整词频列表。
- Emoji 全量分析。
- 词典管理。
- 批量上传和下载。

## 13.3 合并或替换

- `mcd-copy-analyzer` 的 AI 深度改写，合并为统一候选改写服务。
- 两个项目中的 LLM 配置，合并为统一 LLM Gateway。
- 多份禁用词和词典，合并为唯一配置源。
- 多份 CTR 基准，保留一个有效版本和版本号。
- 重复的文件读取和字段映射，抽成共享数据层。

## 13.4 暂不删除

旧项目在新工作台稳定前应继续可独立运行。

第一阶段通过 Adapter 调用或复用现有服务。验证稳定后，再决定是否归档旧 UI。不得在整合初期直接删除旧仓库或旧入口。

## 13.5 Adapter 策略

整合阶段的代码复用必须遵循以下策略：

- **优先 Adapter 直接 import 旧项目纯函数**：避免代码重复，保留单一事实来源。`mcd-ctr-predictor/ctr_predictor.py` 的 `get_baseline_ctr` / `get_time_multiplier` / `build_context_for_llm` / `auto_detect` / `get_char_range` 等函数均为纯函数，可被新项目 `adapters/ctr_predictor_adapter/` 直接 import。
- **thin wrapper 抽取纯函数**：旧模块如强依赖 Streamlit（如 `analyzer.py` 的 `@st.cache_data`），写 thin wrapper 抽取纯 Python 函数；UI 层调用 wrapper，业务层只依赖纯函数。
- **配置文件和 baseline JSON 直接复一份到新项目**：不与旧项目共享，避免双向耦合。`ctr_baseline.json` / `custom_dict.txt` / `stopwords.txt` / `frameworks.json` 在 `data/` 下各保留一份独立副本。
- **旧项目代码风格统一**：抽到 adapter 后用统一命名风格（如 snake_case 函数名 + 类型注解），导入路径标准化（避免跨项目相对路径 `../../`）。
- **禁止复制整个文件**：不得将 `ctr_predictor.py` 或 `app.py` 整体复制进新项目；只复用其纯函数和配置数据。

Adapter 设计的目标是：旧项目任何修改只需更新 adapter 即可，不需要修改新项目的其他层。

---

# 14. 技术选型

## 14.1 MVP 推荐

```text
Streamlit + Python + SQLite
```

理由：

- 现有两个项目使用 Python / Streamlit，复用成本较低。
- 文本分析、Excel、DataFrame、CTR 基准和 LLM 调用均在 Python 生态中。
- 适合内部 Demo 和快速迭代。
- 可以较快实现多页面工作台。

## 14.2 重要限制

Streamlit UI 与业务逻辑必须分离，避免未来无法迁移。

禁止：

- 所有代码都写在 `app.py`。
- 服务函数直接依赖 `st.session_state`。
- 模型调用逻辑写在按钮内部。
- 规则和 Prompt 写死在 UI 文件。
- 数据库操作散落在页面中。

## 14.3 后续升级条件

当出现以下需求时，再考虑 React 或 Vue + FastAPI：

- 多人协作编辑。
- 完整审批流。
- 细粒度权限。
- 复杂富文本编辑。
- 拖拽式内容编排。
- 深度接入正式投放系统。
- 前端体验和埋点要求显著提高。

后续即使更换前端，文本、数据和预测服务仍建议保留 Python。

---

# 15. 推荐工程架构

```text
mcd-ai-content-platform/
├── app.py
├── pages/
│   ├── 01_content_studio.py
│   ├── 02_copy_diagnosis.py
│   ├── 03_batch_evaluation.py
│   └── 04_historical_insights.py
├── core/
│   ├── config.py
│   ├── schemas.py
│   ├── exceptions.py
│   └── llm_gateway.py
├── services/
│   ├── generation_service.py
│   ├── copy_analysis_service.py
│   ├── ctr_prediction_service.py
│   ├── similarity_service.py
│   ├── rule_engine.py
│   └── record_service.py
├── adapters/
│   ├── copy_analyzer_adapter.py
│   ├── ctr_predictor_adapter.py
│   ├── demo_llm_adapter.py
│   └── internal_llm_adapter.py
├── repositories/
│   ├── base.py
│   └── sqlite_repository.py
├── ui/
│   ├── styles.py
│   ├── task_panel.py
│   ├── candidate_panel.py
│   ├── preview_panel.py
│   └── shared_components.py
├── prompts/
│   ├── system_v1.txt
│   ├── generate_candidates_v1.txt
│   └── rewrite_candidate_v1.txt
├── config/
│   ├── app.yaml
│   ├── channel_rules.yaml
│   └── brand_rules.yaml
├── data/
│   ├── custom_dict.txt
│   ├── stopwords.txt
│   └── ctr_baseline.json
├── tests/
│   ├── test_rule_engine.py
│   ├── test_generation_schema.py
│   ├── test_ctr_adapter.py
│   ├── test_similarity.py
│   └── test_state_flow.py
├── requirements.txt
├── .env.example
└── README.md
```

## 15.A 工程化配套

PRD §15 给出 Python 工程结构，但本项目对 Claude Code 协作有额外要求，需配套以下工程化资产：

### 15.A.1 项目记忆与说明

- **`Handoff.md`**（项目记忆）：仿 `mcd-copy-analyzer/Handoff.md`（175 行）范式，必写章节：项目一句话、决策与约束、复用清单、文件落点、设计原则、状态管理、性能、决策记录、待办、Self-check、关键路径速查、新 Session 第一步、教训。每个新 session 第一件事是读此文件。
- **`CLAUDE.md`**（给 AI 看的项目说明）：仿 `mcd-copy-analyzer/CLAUDE.md`（95 行）范式，必写章节：项目一句话、必读、架构概览、关键约束、复用清单、Provider 配置、关键命令、Self-check。

### 15.A.2 Claude Code 协作配置

- **`.claude/agents/`**：sub-agent 配置，包含 3 个：
  - `code-reviewer.md`：按 mcd-analysis skill 5 基线审查（数据准确性 / 口径一致 / 可视化规范 / 工程结构 / 工程化资产）
  - `integration-helper.md`：指导 import 旧模块（按 §13.5 复用策略执行）
  - `test-runner.md`：运行 `tests/verify.py` + `pytest tests/` 并解读输出
- **`.claude/skills/mcd-content-workbench/`**（可选）：项目专属 skill，封装本项目的高频操作（如"新增页面模板"、"新增 prompt 版本"）

### 15.A.3 测试范式

- **`tests/verify.py`**：仿 `mcd-ctr-predictor/tests/verify.py` 模式，无 pytest 依赖，ast.exec 注入 namespace。33+ 用例覆盖关键纯函数。
- **`tests/test_*.py`**：pytest 单元测试，针对解耦后的纯函数（如 `rule_engine` / `ctrr_adapter` / `generation_schema` / `similarity` / `state_flow`）。
- **测试入口约定**：单测用 pytest 跑（开发期快速反馈），集成验证用 verify.py 跑（无需装 pytest）。

### 15.A.4 一键启动

- **`setup_and_run.bat`**：仿 `mcd-reach-trend/setup_and_run.bat` 范式（venv + 国内 pip 镜像 + 依赖检查 + streamlit run app.py）。双击即启动。

### 15.A.5 交付要求

首批交付必须包含以上所有工程化资产，**不允许"开发完再补"。** 工程化资产与代码同步演化。

---

# 16. 开发前代码审阅任务

Claude Code 在写新功能前，必须输出一份简短审阅结果：

## 16.1 mcd-copy-analyzer

逐项说明：

- 可以直接复用的模块。
- 需要解耦后复用的模块。
- 与 Streamlit 强耦合的模块。
- 重复或可以废弃的模块。
- 关键配置和数据文件。
- 当前测试覆盖情况。

## 16.2 mcd-ctr-predictor

逐项说明：

- 当前实际维护版本。
- CTR 基准文件结构。
- 预测逻辑是规则、LLM、统计模型还是组合方式。
- 是否返回单点值、区间、准确率或其他指标。
- 是否存在样本量和校准信息。
- 可以直接复用的函数。
- 需要适配的输入和输出。

## 16.3 审阅输出格式

```text
模块名称
- 当前作用
- 建议处理：直接复用 / 抽取 / 重构 / 保留旧页面 / 暂不使用
- 原因
- 新项目目标位置
- 风险
```

审阅完成后直接按本 PRD 开发 P0，无需等待再次确认；如果实际代码与 PRD 假设冲突，应在 README 的“实现差异”中记录，并采用最小、真实、可解释的实现。

---

# 17. 状态管理

使用 `st.session_state` 仅保存页面会话状态：

```python
{
    "task_input": {},
    "task_snapshot_hash": "",
    "candidates": [],
    "selected_candidate_id": "A",
    "edited_candidates": {},
    "rule_results": {},
    "similar_results": {},
    "prediction_results": {},
    "generation_status": "idle",
    "request_id": None,
    "app_mode": "demo"
}
```

要求：

- 切换候选不重新生成。
- 人工编辑不丢失。
- 左栏字段改变后提示重新生成。
- 预测失败不清除候选。
- 规则检查在人工编辑后重新执行。
- CTR 预测是否自动重算应配置化，避免每次输入字符都调用 API。

---

# 18. 数据保存

## 18.1 MVP

使用 SQLite，封装 Repository 层。

## 18.2 保存字段

- 请求 ID。
- 创建时间。
- 创建人或占位用户。
- 完整经营任务。
- 三条 AI 原始候选。
- 人工编辑后的候选。
- 最终选中版本。
- 规则检查结果。
- 历史相似内容引用。
- CTR 结果和 `result_type`。
- CTR 基准版本。
- 模型版本。
- Prompt 版本。
- 保存状态。

## 18.3 后续效果回流

预留字段：

- 实际投放日期。
- 实际渠道。
- 实际触达。
- 实际点击。
- 实际 CTR。
- 实际转化。
- 预测偏差。

MVP 不要求自动接入投放系统，但数据库结构应允许后续扩展。

---

# 19. Demo 模式与真实模式

## 19.1 Demo 模式

```text
APP_MODE=demo
```

要求：

- 无外部 API 也能完整运行。
- 可以生成 3 条稳定候选。
- 可以完成选择、编辑、预览、规则检查和保存。
- CTR 卡片明确标注 Demo。
- 不使用每次刷新都会变化的随机数。

## 19.2 内部模型模式

```text
APP_MODE=internal_llm
```

要求：

- 从环境变量读取服务地址和凭证。
- 统一使用 LLM Gateway。
- 有超时、有限重试和错误处理。
- 不向前端暴露堆栈、密钥或内部 Token。

## 19.3 CTR 模式

建议独立配置：

```text
CTR_MODE=existing_predictor
CTR_MODE=baseline_only
CTR_MODE=demo
```

---

# 20. 视觉要求

## 20.1 主页面

尽量忠实还原领导 Demo：

- 左侧浅色任务面板。
- 中间深色候选区。
- 选中卡片使用黄色描边。
- 右侧大尺寸预览。
- 预览下方为 CTR、理由、可信程度三个结果卡片。
- 顶部显示 1、2、3 步骤编号。

## 20.2 品牌视觉

- 主色：红、金、深灰、米白。
- 金色用于主按钮和选中态。
- 深灰用于候选背景。
- 红色只用于品牌点缀和阻断风险，不大面积滥用。
- 卡片圆角保持统一。
- 页面结果层级清晰，避免堆太多指标。

## 20.3 结果简化

首页只突出：

1. 文案状态：通过 / 提醒 / 阻断。
2. CTR 参考。
3. 投放理由。
4. 参考可信度。

详细词频、趋势、Owner 对比等移入独立页面。

---

# 21. 异常处理

| 场景 | 处理 |
|---|---|
| 必填字段为空 | 阻止生成，在对应字段显示提示 |
| LLM 超时 | 保留输入，显示可重试信息 |
| LLM JSON 不合法 | 有限修复一次，失败后终止本次生成 |
| 候选不足 3 条 | 视为失败，不展示残缺候选 |
| 文案命中阻断规则 | 红色展示，不给出建议投放结论 |
| 历史检索失败 | 候选仍可使用，历史证据显示不可用 |
| CTR 服务失败 | 其他模块正常，CTR 显示不可用及原因 |
| 无匹配基准 | 不猜测，显示无足够基准数据 |
| 保存失败 | 保留页面状态，允许重新保存 |
| 配置丢失 | 启动时给出明确配置错误 |

---

# 22. P0、P1、P2 优先级

## P0：领导 Demo 与最小闭环

- 三栏主页面。
- 六个核心经营任务字段。
- 更多预测条件折叠区。
- 三条差异化候选。
- 候选选择。
- 标题与正文编辑。
- APP Push 预览。
- 本地规则检查。
- CTR Adapter 和结果卡片。
- 投放理由。
- 参考可信度。
- Demo 模式。
- SQLite 保存。
- 现有项目代码审阅结果。

## P1：现有能力整合

- 企微 1v1、短信和站内信预览。
- 单条重新生成和统一改写。
- 历史相似 Plan。
- 历史高表现表达参考。
- 候选差异检查。
- 文案诊断独立页面。
- 批量评估独立页面。

## P2：完整平台化

- 历史洞察完整页面。
- 设置管理界面。
- 企业统一认证。
- 角色权限。
- 审核流程。
- 实际投放结果回流。
- 模型校准和误差监控。
- 正式投放系统对接。
- 必要时迁移到 React / Vue + FastAPI。

---

# 23. P0 验收标准

## 23.1 页面和交互

- [ ] 可以通过 `streamlit run app.py` 启动。
- [ ] 主页面在宽屏下为左、中、右三栏。
- [ ] 领导 Demo 中的核心字段均保留。
- [ ] 点击生成后出现 A、B、C 三条不同策略候选。
- [ ] 默认选中 A。
- [ ] 切换 B 或 C 时右侧立即变化。
- [ ] 切换候选不会重新生成，也不会丢失编辑结果。
- [ ] 可以编辑标题和正文。
- [ ] 右侧展示 APP Push 预览。
- [ ] 右侧展示 CTR 参考、投放理由和参考可信度。
- [ ] CTR 结果明确标记类型：预测、历史基准、Demo 或不可用。
- [ ] 阻断规则命中时不得显示建议投放。
- [ ] 可以保存完整任务和结果。

## 23.2 工程

- [ ] 已审阅两个现有项目并记录复用建议。
- [ ] 新项目未简单复制三个项目到一个文件。
- [ ] UI、服务、适配器、存储分层。
- [ ] Prompt、渠道规则、品牌规则可配置。
- [ ] Copy Analyzer 和 CTR Predictor 通过 Adapter 接入。
- [ ] Demo 模式不依赖外部 API。
- [ ] API Key 不进入仓库。
- [ ] 核心规则、Schema、Adapter 有测试。
- [ ] README 说明安装、运行、模式切换和实现差异。

---

# 24. 测试清单

## 24.1 规则测试

- APP Push 标题长度。
- 正文长度。
- 禁用词。
- 未提供数字。
- 未提供日期。
- 未确认优惠或免费承诺。
- 候选相似度。

## 24.2 CTR Adapter 测试

- 完整条件命中基准。
- 部分条件缺失。
- 无匹配基准。
- 旧预测器返回异常。
- 单点预测不被伪装为区间。
- Demo 结果稳定。

## 24.3 交互测试

- 生成 3 条候选。
- 切换候选。
- 编辑后切换再返回。
- 左侧任务改变后提示重新生成。
- 预测失败但文案正常展示。
- 保存和重新读取。

---

# 25. Claude Code 执行指令

将本文件与三个项目目录放在同一工作区，然后向 Claude Code 提供以下指令：

```text
请根据《MCD AI 内容运营工作台需求文档 v2.0》开发项目。

首要要求：
1. 先阅读完整 PRD。
2. 先审阅 mcd-copy-analyzer 和当前有效的 mcd-ctr-predictor 项目。
3. 输出模块复用清单，再直接开始实现 P0，不需要等待确认。
4. 领导 Demo 的三栏结构和核心功能必须保留。
5. 除非存在真实性、合规、技术不可行或明显重复问题，不得擅自删除领导 Demo 功能。
6. 如果 CTR 项目不支持真实区间或准确率，保留卡片位置，但使用真实可解释的“CTR 参考”和“参考可信度”。
7. 不要把旧项目代码简单复制进一个大 app.py。
8. 使用 Adapter 隔离旧项目，服务层不得依赖 Streamlit UI。
9. 第一版必须支持 APP_MODE=demo，无外部 API 也能跑通完整流程。
10. 优先完成 P0，P1 与 P2 只做接口和必要占位。
11. 提供 requirements.txt、.env.example、README.md、配置文件和核心测试。
12. 完成后实际运行测试，并检查启动、生成、选择、编辑、预览、诊断、CTR 结果和保存链路。
13. 将实际代码与 PRD 不一致之处写入 README 的“实现差异”，不得用假数据掩盖能力缺口。
```

---

# 26. 待业务确认项

以下信息不阻塞 P0 开发，可以先采用本文默认值并配置化：

1. 第一场领导 Demo 的主渠道是否为 APP Push。
2. 目标人群的正式枚举值。
3. 活动阶段的正式枚举值。
4. 消费场景的正式枚举值。
5. 渠道标题和正文字数上限。
6. 品牌禁用词、风险词和必带字段清单。
7. 当前正式维护的是 `mcd-ctr-predictor` 还是 `mcd-ctr-predictor-main`。
8. CTR Predictor 当前结果是否经过正式校准。
9. 是否有样本量、误差、置信区间或离线验证指标。
10. 内网 LLM 的接口规范和认证方式。
11. 是否需要在第一版支持企微 1v1 预览。
12. 生成记录是否允许存储完整文案和用户信息。

实现原则：以上内容应放在配置或 Adapter 中，不应硬编码，后续确认时可以快速替换。

---

# 27. 最终产品判断

本项目应呈现为一个统一工具，但不是一个不可维护的巨型页面。

正确形态：

```text
一个访问入口
+ 一个领导 Demo 主流程
+ 多个独立业务页面
+ 一套共享规则与数据
+ 两个现有项目的能力适配层
```

领导 Demo 负责定义用户最直接看到的价值：

> 选择经营任务，自动生成候选文案，比较并预览结果。

现有两个项目负责给这个 Demo 增加真实业务基础：

> 历史文案分析提供证据和规则，CTR Predictor 提供基准和预测参考。

最终目标不是把三个项目机械合并，而是把“历史洞察、内容生成、效果参考和人工决策”连接成一个完整的内容运营闭环。
